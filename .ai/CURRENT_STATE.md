# Current state — 2026-09-01

- Публичное название продукта — **SlovoKrok**; GitHub repository URL,
  browser-storage keys, API routes, SQLite table names и course identifiers
  сохранены.

- `/` — единственный канонический интерактивный Slovak A1; `/new` и
  `/module-1` перенаправляют на него.
- Frontend содержит 8 модулей и 83 урока; Modules 1–6 имеют подробный контент
  с пошаговой практикой, проверками и тематическими группами. Module 3
  содержательно сверён с шестью документами владельца (42 страницы), Module 4
  — с 11 документами (77 страниц); Modules 5–6 пока остаются
  лингвистически неутверждёнными черновиками.
- Все 83 темы Modules 1–8 хранятся по одной в файле
  `frontend/app/data/modules/moduleN/lessons/<slug>.ts`; общая логика каждого
  модуля вынесена в `lessonFactory.ts`, порядок — в `index.ts`.
- В `ROADMAP.md` записан четырёхэтапный архитектурный план. До его выполнения
  созданы и проверены snapshot 193 кандидатных файлов и Git bundle полной
  истории в ignored `_git-package/checkpoints/`.
- Первый пункт этапа 1 завершён: чистая логика проверки ответов, парных
  заданий, закрепления и итоговых вопросов вынесена из `CourseScreen.tsx` в
  `frontend/app/data/coursePractice.ts`; UI/API/SQLite и идентификаторы не
  менялись.
- В теме 1 задания закрепления 3 и 5 переведены с единого точного текстового
  ответа на построчную проверку: четыре исправленные подсказки и шесть
  переводов вводятся в отдельных полях. Для `ja` принимаются `я`, `йа` и
  `я(йа)`.
- Тема 2 сокращена до пяти учебных шагов: шаг 5 содержит частые ошибки, а
  прежние упражнения и самопроверка заменены отдельным финальным тестом темы
  из шести интерактивных заданий.
- Владелец проверил и согласовал все 14 тем Module 1, включая материалы,
  упражнения и тесты. Ручная приёмка Module 1 завершена; следующий продуктовый
  этап — Module 2.
- Активные frontend/backend имена универсальны (`Course*`), FastAPI монтирует
  `/health`, tutor chat/settings/Codex login и `/api/v1/course/*`.
- Экран «Настройки ИИ» переключает Codex CLI, OpenAI и Polza. API-ключи
  сохраняются только локально в ignored `backend/data/ai_settings.json` и не
  возвращаются браузеру.
- SQLAlchemy-модели называются `Course*`; прежние физические имена
  `module1_beta_*` сохранены только как контракт существующей SQLite-базы.
- Прежние browser-storage key сохранены как fallback импорта; в UI, маршрутах,
  компонентах, типах и API временной приписки больше нет.
- Classic-код, исторические документы и локальные/generated материалы вынесены
  за пределы active tree; пользовательская резервная копия не является source
  of truth.
- `_git-package/AI-Learning-Platform-2026-08-26` остаётся локальной резервной
  выборкой; каноническим Git-кандидатом является прошедшее аудит рабочее дерево.
- Compact runtime опубликован в `origin/main` коммитом `3214334`; локальный и
  удалённый hash совпали, GitHub CI завершился успешно.
- После первого архитектурного шага подтверждены content/TypeScript validation,
  Next.js build, Playwright `29 passed`, backend `11 passed` и compileall.
  Ручная приёмка владельцем Modules 3–4 по-прежнему ожидается.
- `npm audit --omit=dev` сообщает 6 high advisories в зависимостях
  Next.js/Playwright; автоматическое breaking-обновление не применялось.
- Git-история подтверждает прежнюю строку формата API-ключа в `.env.example`.
  Значение не читалось; отзыв credential владельцем не подтверждён.
- Следующий архитектурный шаг начинается в `CourseScreen.tsx`: свести учебное
  состояние к typed actions и отделить persistence/hydration от UI без
  изменения CourseState v1. Параллельная продуктовая задача владельца — ручная
  проверка Modules 3–4 на `/`; замечания фиксировать по module/lesson slug и
  исправлять без изменения activity ID.
- `course-content/slovak-a1/learning/learning_roadmap.md` имеет отдельные
  незакоммиченные изменения; считать их пользовательскими и не откатывать.
